const form = document.getElementById('form-calculadora')

form.addEventListener('submit', function(e){
    e.preventDefault()

    const nomeAtividade = document.getElementById('nome-atividade')
    const notaAtividade = document.getElementById('')
})